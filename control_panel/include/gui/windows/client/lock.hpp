#pragma once

namespace GUI {
    namespace Windows {
        enum class Lock {
            Released,
            Calibrate,
            Measure,
            Auto,
            UnexpectedDisconnection,
        };
    }
}